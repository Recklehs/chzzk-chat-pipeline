APP_ENV=test
HOST=0.0.0.0
PORT=8000

# Raw event backend selector. Connection values live in `.env.test` / `.env.prod`.
EVENT_BUS_BACKEND=kafka
# EVENT_BUS_BACKEND=pubsub

# Expose Prometheus metrics endpoint. Keep disabled for normal local runs unless needed.
METRICS_ENABLED=false
